program constreal(output);
const
  pi = 3.14159265;
  msg = 'ok';
  limit = 100;
var r : real; i : integer;
begin
  r := pi;
  writeln(r);
  writeln(msg);
  i := limit;
  writeln(i);
  r := -pi;
  writeln(r)
end.
