program stdlib3(output);
var r: real; i: integer;
begin
  r := exp(0.0);
  writeln(r:20);
  r := arctan(0.0);
  writeln(r:20);
  r := sin(0.0);
  writeln(r:20);
  r := cos(1.0);
  writeln(r:20);
  i := trunc(sqrt(2.0));
  writeln(i)
end.
