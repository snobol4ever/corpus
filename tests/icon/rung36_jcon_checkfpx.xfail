runs to completion (rc=0) but stdout does not match .expected. Not further diagnosed. UPDATE 2026-08-30 (seat01): looks real-formatting-shaped on the surface (e.g. "0.796084e-1" vs expected "0.07960", scientific vs decimal notation) but CONFIRMED NOT the same root cause as the cset()/list_bang_at real_str-vs-icon_real_str bug class fixed this same day elsewhere in rung36_jcon_* -- re-checked after those landed, this file's diff is byte-for-byte unchanged. A different, still-uninvestigated cause.
