procedure main();
   local s;
   s := "-n10";
   s ? {
      write("first *subj=", *&subject, " subj=[", &subject, "]");
   };
   s ? {
      if ="-" then write("blk1 matched");
   };
   s ? {
      write("second *subj=", *&subject, " subj=[", &subject, "]");
   };
end
