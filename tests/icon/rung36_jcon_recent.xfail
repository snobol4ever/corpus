was "exits rc=1, stdout does not match .expected. Not further diagnosed" -- the rc=1 was ALSO true
before a parse error (the semicolon-conversion split a 'while EXPR do STMT' by wrongly terminating
EXPR with ';' before 'do', corpus/tests/icon/rung36_jcon_recent.icn line 215), fixed seat03 2026-09-05
(corpus, this push). NOW parses+runs further, rc=1, TWO separate real issues found: (1) a list-element
ORDERING difference (a "list ... 1 2 3 4 5 95 96 97 98 99 100" line appears at a different position) --
possibly the existing icon-arizona-class-table-iteration-order-not-insertion DO-NOT-TOUCH class, not
independently confirmed this session; (2) UPDATE 2026-09-06 (seat06): the "via forward-referenced procedures dump()/same()" attribution below
is WRONG -- re-traced with SCRIP_DEBUG_APPLY=1, the actual unresolved name at the point of failure is
`sortf` (`[call-err5] unresolved 'sortf' (ufname='sortf', nargs=2)`, from `test()`'s `r1 := sortf(data,
n)`, line 263), not `dump`/`same` (which sit later in the same function and are never reached). `data` is
a list mixing record instances with plain integers/lists/&null -- this is the SAME mixed-type sortf gap
now rowed as `icon-sortf-crashes-on-mixed-record-and-scalar-lists` (rank 1, hq_B: confirmed SEGV on one
mixed shape, wrong-order-no-crash on a smaller one). NOT this row's witness in
`icon-jcon-sorting-randval-undefined-function-error` after all -- that task's own `randval()`/case-arm
hypothesis is separately refuted on ITS OWN file this same session (see `rung36_jcon_sorting.xfail`);
cross-posted there rather than re-minted here since the mixed-list sortf row already exists.
