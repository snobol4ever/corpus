rc=139 SIGSEGV in BOTH modes at line 113 (`=[]`), AFTER the program compiles and runs 112 lines. NOT an output mismatch.
SIGNATURE: memcmp in scan_match on a non-string &subject. The subject was poisoned at line 92: `[] ? []` installs a
list as &subject and returns list_8(0) where icont raises 103 "string expected" BEFORE scanning; `(&subject := [])` at
line 90 fails silently where icont raises 103. Cure the scan box's subject check (103 before install) and the &subject
keyword store, and the crash goes with them.
ALSO RED, both modes, builtin class: `seq("a")` returns 0 (icont: 101); `"|"(1,2)` reports error 22 "Undefined function
called" (icont: 106 — string invocation of an operator name, hq_U's named class).
HISTORY: the m4 compiler abort (FATAL emit_drive op=16 then op=122) was cured 2026-09-09 by the cto — `()` is &null,
a literal side of :=: / <- / <-> raises 111 — see FINDING-2026-09-09-cto-the-empty-expression-and-a-literal-swap-operand-had-no-box-so-the-compiler-aborted-where-icon-raises-111-and-102.md.
Lon 2026-09-03: there is no XFAIL. This marker is a ROUTING SLIP, deleted when the program passes.
