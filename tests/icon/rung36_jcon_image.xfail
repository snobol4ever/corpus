UPDATE 2026-09-06 (seat06): the "likely third witness of the sorting/randval class" lead in seat03's
2026-09-05 note below is REFUTED -- bisected with SCRIP_DEBUG_APPLY=1 to the exact call, and `dump(x)`'s
forward-reference has nothing to do with it: `dump`'s BODY calls Icon's `serial(x)` builtin, and SCRIP's
diagnostic says plainly `[call-err5] unresolved 'serial' (ufname='serial', nargs=1)` -- `serial()` is
simply not implemented in SCRIP at all (any argument, any context, confirmed in total isolation). This is
a THIRD, UNRELATED mechanism from both `sorting.icn` and `recent.icn`'s real causes (see their own marker
updates, same date) -- all three merely SHARE the generic "(0) : ERROR 022 -- Undefined function called"
surface message, which is exactly why they looked like one class before anyone traced the actual call.
Rowed as `icon-serial-builtin-is-entirely-unimplemented` (rank 2, hq_B, DONE-WHEN exercised red before
trusting).
UPDATE 2026-09-05 (seat03), KEPT FOR THE RECORD, mechanism corrected above: marker text corrected AND
likely-classified. Actual current behavior: SCRIP exits rc=1 with `(0) : ERROR 022 -- Undefined function
called` (repeated twice, "in statement 0" both times) -- NOT a benign rc=1/output-mismatch as the old text
implied; nothing else runs.
