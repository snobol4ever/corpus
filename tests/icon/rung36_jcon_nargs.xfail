UPDATE 2026-09-05 (seat03): ablated (fresh oracle vs SCRIP, first divergence only, not fully root-caused).
FIRST DIVERGENCE: SCRIP reports NEGATIVE argument counts for built-in functions where real icont/iconx
reports positive ones -- ` -1 acos` vs `  1 acos`, ` -2 any` vs `  4 any`, ` -1 asin` vs ... (pattern
continues). This is a NEW, deterministic, reproducible divergence in the nargs-per-builtin count itself --
NOT (at this first divergence) the previously-flagged non-reproducible-value issue
(`FINDING-2026-09-04-hq_B-...`: "nargs a file-descriptor number ( -3 open vs  2 open)"), which if still
present would show up FURTHER DOWN this same file's output and was not re-checked this session (per "read
the FIRST divergence, don't batch-assume" -- stopping here rather than assuming the two are the same
mechanism or that the fd issue is resolved). Untested hypothesis, not chased: Icon's own convention may use
a negative count to mean "variadic, at least N" for some C-implemented builtins vs a positive exact count
for others, and SCRIP's sign convention differs from the oracle's -- or SCRIP may simply be reporting the
count with an inverted sign uniformly. Whoever picks this up should re-derive the fd-non-reproducibility
question fresh (the row's own law) rather than trust the 2026-09-04 census unchanged.
