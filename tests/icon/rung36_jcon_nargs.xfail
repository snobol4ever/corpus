UPDATE 2026-09-06 (seat06): ROOT-CAUSED AND ROWED, not just first-divergence classified. 60 of 86 builtins'
reported arg counts diverge from real icont/iconx (9.5.25a); the wrong-sign-only theory from the prior note
is INCOMPLETE -- most mismatches are wrong MAGNITUDE too (e.g. `any`/`many`/`match`/`upto`/`find` should be
4, `bal` should be 6, SCRIP reports a flat -2 for most of these). Full mechanism (three sub-paths, all in
`by_name_dispatch.c`'s BID_args handler, src/runtime/by_name_dispatch.c:4698-4743) and the complete side-by-
side evidence are in the new row's own task file, not repeated here. Previously-flagged non-reproducible fd
question (`open`: SCRIP=2, oracle=-3) is ALSO covered by this same row -- it is not a separate non-
reproducible-value class, it is the SAME arity-table defect (open should be reported variadic, -3, and
isn't). Rowed as `icon-args-builtin-arity-table-is-wrong-or-missing-for-most-builtins` (rank 2, hq_B),
DONE-WHEN exercised red before trusting.
