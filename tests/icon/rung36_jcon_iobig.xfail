rc=139 SEGV (core dumped) — NOT an output mismatch and NOT rc=1.
SIGNATURE: rc=139 SEGV, no diagnostic. ABLATED 2026-09-03 to an 8-line witness with NO file I/O and NO large
           data:  inside a CO-EXPRESSION. Each ingredient is clean alone (cset-keyword
           generation without a co-expression: rc=0; a co-expression over a string literal: rc=0); only the
           INTERSECTION faults. icont runs it and prints 0..9. ⛔ The name "iobig" is misleading: reducing
           textgen's repl from 65536 to 16 leaves the SEGV unchanged, so neither size nor I/O is involved.
ROUTING:   icon-cset-keyword-element-generation-inside-a-coexpression-segvs (the defect).
           Also icon-arizona-class-silent-segv-no-diagnostic — curing THAT would at least make this one speak.

RE-CLASSED 2026-09-03 (hq_B, row icon-every-xfail-fixed-as-a-faulty-test-or-cured-as-a-defect).
The previous text read: 'exits rc=1 (stdout does not match .expected). Not further diagnosed.'
That was measured stale IN THE DANGEROUS DIRECTION: it described a CRASH as a mild output mismatch,
so anyone triaging by marker text would sort three crashes to the bottom of an output-diff pile.
Lon 2026-09-03: there is no XFAIL. This marker is a ROUTING SLIP toward a named cure, not a resting
place -- it is deleted when the row it names goes green.
