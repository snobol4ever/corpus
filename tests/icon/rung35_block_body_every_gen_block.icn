procedure main();
  L := [10, 20, 30];
  every v := !L do {
    w := v + 100;
    write(w);
  };
  write("break");
  every v := !L do {
    if v = 20 then break;
    write(v);
  };
  write("next");
  every v := !L do {
    if v = 20 then next;
    write(v);
  };
end
