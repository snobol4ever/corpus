* 211_indirect_assign.sno — indirect assignment and undefined name
* Rung 2. 2 assertions.

*       assign through indirect: $'qq' = 'x'  sets variable qq
        $'qq' = 'x'
        DIFFER(qq, 'x')                   :f(e001)
        OUTPUT = 'FAIL 211/001: indirect assign sets named var' :(END)
e001

*       lookup of undefined variable via $ fails (returns null, which DIFFER succeeds on)
        DIFFER($'_no_such_var_')                   :f(e002)
        OUTPUT = 'FAIL 211/002: undefined indirect is null'     :(END)
e002

        OUTPUT = 'PASS 211_indirect_assign (2/2)'
END
