procedure main()
  s := "abc";
  every s[2] <- "XY" do write(s);
  write(s);
  if s[2] <- "Z" then write(s);
  write(s);
  r := "pqr";
  (r[5] := "!") | write("oob-failed");
  write(r);
  (r[0] := "!") | write("zero-failed");
  write(r)
end
