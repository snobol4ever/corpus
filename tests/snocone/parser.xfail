/*-------------------------------------------------------- 19 break_for XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 20 break_nested XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*------------------------------------------------------ 21 break_while XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 28 continue_for XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*--------------------------------------------------- 29 continue_while XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 30 do_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 31 do_with_stmt XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 32 func_args XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 33 func_body XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------- 34 func_def_call XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 36 func_freturn XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 37 func_nreturn XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------- 38 func_one_assign XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*------------------------------------------------------ 39 func_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------- 40 func_three_args XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*--------------------------------------------------- 41 func_two_funcs XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------------- 42 if_else XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*------------------------------------------------------- 43 if_else_if XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------- 44 if_else_if_else XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------- 45 if_else_if_else_if XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------- 46 if_multi_body XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------------- 47 if_seq XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 48 if_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 62 switch_break XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*--------------------------------------------------- 63 switch_default XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------- 64 switch_fallthrough XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------- 65 switch_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 66 while_seq XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 67 while_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. RULED 2026-08-29 by hq_C (row family5-attr-adjudication-needed, live-measured, corrects an earlier seat02 draft that lumped both attributes together -- see .github/FINDING-2026-08-29-seat02-family5-incl-src-is-a-snocone-synthesis-bug-not-deliberate.md, updated in place with hq_C's ruling): SPLIT -- :src is DELIBERATE (806508fd's per-statement source capture; nothing exempts a block body), :incl is A BUG, precisely located: 764752c6 labelled the ENTIRE stmt_src_slice()-returned-NULL branch ":incl", but 806508fd's own Known limits already documented that branch has more than one cause ("second statement on a ';' line and included-file statements are guarded to silence") -- so every non-INCLUDE slice failure now falsely asserts INCLUDE provenance, and drags a false :src "(INCLUDE)" fallback text along with it (same two lines set both). Measured live on a zero -INCLUDE Snocone file: every block-body statement in both .sc and .sno carries this false pair; top-level statements carry neither. NOT the lineno off-by-one stmt-src-slice-bare-label-lineno-off-by-one-false-include-attr already cured -- linenos are correct. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Do NOT promote: the compiler must first stop emitting :incl (and :src's false fallback text) for any !ssrc cause other than a genuine include, at the one site in stmt_ast.c; only then do the 29 refs get re-derived and the XFAILs clear. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
