/*-------------------------------------------------------- 19 break_for XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 20 break_nested XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*------------------------------------------------------ 21 break_while XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 28 continue_for XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*--------------------------------------------------- 29 continue_while XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 30 do_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 31 do_with_stmt XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 32 func_args XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 33 func_body XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------- 34 func_def_call XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 36 func_freturn XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 37 func_nreturn XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------- 38 func_one_assign XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*------------------------------------------------------ 39 func_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------- 40 func_three_args XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*--------------------------------------------------- 41 func_two_funcs XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------------- 42 if_else XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*------------------------------------------------------- 43 if_else_if XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------- 44 if_else_if_else XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------- 45 if_else_if_else_if XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------- 46 if_multi_body XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------------- 47 if_seq XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 48 if_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 62 switch_break XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*--------------------------------------------------- 63 switch_default XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------- 64 switch_fallthrough XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*---------------------------------------------------- 65 switch_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*-------------------------------------------------------- 66 while_seq XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
/*----------------------------------------------------- 67 while_simple XFAIL */
Family 5 (:incl/:src provenance attrs) reverted to red-and-known per the reconciled hq_P ruling (msg ruling-option-b-scoped-revert-family-5-only) and ceo's amendment (msg re-family5-one-voice), snocone-parser-fixture-ast-drift-ruling.task.md. Neither a source-read of stmt_ast.c's stmt_src_slice() nor runtime execution can settle whether the live compiler's TT_ATTR :incl/:src attributes on block-body statements are deliberate or a bug -- that requires the attr-introduction commit, which is git archaeology reserved to hq_C. This entry's .ref is restored to the pre-regen (corpus a0b3e603) shape for those two attributes only; every other shape in this entry (assign-wrap/augop/n-ary-chain/return-fusion, all ratified) is UNCHANGED from the live-compiler regen. Expected to fail against the live compiler until hq_C rules -- that failure is the point, not a defect. Promote (remove XFAIL + delete this reason block) only after hq_C rules the attribute deliberate; otherwise the ref entry itself should drop the :incl/:src re-addition and this reason block, matching whatever hq_C's actual ruling turns out to require of the shape.
