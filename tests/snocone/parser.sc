/*--------------------------------------------------------------- 1 arith_add */
x = 1 + 2;
/*--------------------------------------------------------------- 2 arith_div */
x = 10 / b;
/*--------------------------------------------------------------- 3 arith_mul */
x = a * b;
/*--------------------------------------------------------------- 4 arith_sub */
x = a - 3;
/*---------------------------------------------------------- 5 arith_sub_nary */
x = a - b - c;
/*-------------------------------------------------------------- 6 assign_int */
x = 5;
/*------------------------------------------------------------ 7 assign_mixed */
x = 42;
y = "hello";
z = x;
w;
/*-------------------------------------------------------------- 8 assign_seq */
a = 1;
b = 2;
/*-------------------------------------------------------------- 9 assign_str */
x = "hi";
/*------------------------------------------------------------- 10 assign_var */
x = y;
/*---------------------------------------------------------------- 11 atom_id */
x;
/*--------------------------------------------------------------- 12 atom_int */
42;
/*--------------------------------------------------------------- 13 atom_str */
"hello";
/*---------------------------------------------------------- 14 augmented_add */
x += 1;
/*---------------------------------------------------------- 15 augmented_div */
x /= 4;
/*---------------------------------------------------------- 16 augmented_mul */
x *= 3;
/*---------------------------------------------------------- 17 augmented_pow */
x ^= 2;
/*---------------------------------------------------------- 18 augmented_sub */
x -= 2;
/*-------------------------------------------------------- 19 break_for XFAIL */
for (i = 0; LT(i, 10); i += 1) {
    break;
}
/*----------------------------------------------------- 20 break_nested XFAIL */
while (EQ(x, 1)) {
    while (EQ(y, 2)) {
        break;
    }
}
/*------------------------------------------------------ 21 break_while XFAIL */
while (EQ(x, 1)) {
    break;
}
/*------------------------------------------------------------- 22 call_noarg */
x = f();
/*----------------------------------------------------------- 23 call_one_arg */
x = g(42);
/*------------------------------------------------------------ 24 call_simple */
x = f(1, 2);
/*-------------------------------------------------------------- 25 call_stmt */
print(x);
/*------------------------------------------------------------- 26 call_three */
x = h(a, b, c);
/*------------------------------------------------------------- 27 concat_seq */
x = "hello" name;
/*----------------------------------------------------- 28 continue_for XFAIL */
for (i = 0; LT(i, 10); i += 1) {
    continue;
}
/*--------------------------------------------------- 29 continue_while XFAIL */
while (EQ(x, 1)) {
    continue;
}
/*-------------------------------------------------------- 30 do_simple XFAIL */
do { y = 1; } while (x);
/*----------------------------------------------------- 31 do_with_stmt XFAIL */
x = 0;
do { x = x + 1; } while (x);
/*-------------------------------------------------------- 32 func_args XFAIL */
function f(a, b) { return a; }
/*-------------------------------------------------------- 33 func_body XFAIL */
function f(a, b) { c = a + b; return c; }
/*---------------------------------------------------- 34 func_def_call XFAIL */
function f(a, b) { return a + b; }
x = f(1, 2);
/*------------------------------------------------------------- 35 func_empty */
function f() { }
/*----------------------------------------------------- 36 func_freturn XFAIL */
function p() { freturn; }
/*----------------------------------------------------- 37 func_nreturn XFAIL */
function q() { nreturn; }
/*-------------------------------------------------- 38 func_one_assign XFAIL */
function f() { x = 1; }
/*------------------------------------------------------ 39 func_simple XFAIL */
function f() { return; }
/*-------------------------------------------------- 40 func_three_args XFAIL */
function g(a, b, c) { return c; }
/*--------------------------------------------------- 41 func_two_funcs XFAIL */
function p() { return; }
function q(x) { return x; }
/*---------------------------------------------------------- 42 if_else XFAIL */
if (a) { x = 1; } else { x = 2; }
/*------------------------------------------------------- 43 if_else_if XFAIL */
if (EQ(x, 1)) { y = 2; } else if (EQ(x, 3)) { y = 4; }
/*-------------------------------------------------- 44 if_else_if_else XFAIL */
if (EQ(x, 1)) { y = 1; } else if (EQ(x, 2)) { y = 2; } else { y = 3; }
/*----------------------------------------------- 45 if_else_if_else_if XFAIL */
if (EQ(x, 1)) { y = 1; } else if (EQ(x, 2)) { y = 2; } else if (EQ(x, 3)) { y = 3; }
/*---------------------------------------------------- 46 if_multi_body XFAIL */
if (x) { a = 1; b = 2; }
/*----------------------------------------------------------- 47 if_seq XFAIL */
x = 0;
if (x) { y = 1; }
z = 2;
/*-------------------------------------------------------- 48 if_simple XFAIL */
if (x) { y = 1; }
/*--------------------------------------------------------------- 49 scan_alt */
x ? 'foo' | 'bar';
/*------------------------------------------------------- 50 scan_alt_replace */
x ? 'a' | 'b' = 'z';
/*----------------------------------------------------------- 51 scan_in_expr */
y = x ? 'foo';
/*------------------------------------------------------------- 52 scan_juxta */
x  'foo';
/*----------------------------------------------------- 53 scan_juxta_replace */
x  'foo' = 'bar';
/*------------------------------------------------------- 54 scan_juxta_three */
x  'a'  'b';
/*--------------------------------------------------------- 55 scan_multi_pat */
x ? 'a' 'b' 'c';
/*----------------------------------------------------------- 56 scan_pat_var */
x ? p 'foo';
/*----------------------------------------------------------- 57 scan_replace */
x ? 'foo' = 'bar';
/*------------------------------------------------------------ 58 scan_simple */
x ? 'foo';
/*----------------------------------------------------------- 59 struct_empty */
struct Empty {  }
/*---------------------------------------------------------- 60 struct_fields */
struct Person { name, age, email }
/*---------------------------------------------------------- 61 struct_simple */
struct Point { x, y }
/*----------------------------------------------------- 62 switch_break XFAIL */
switch (x) {
case 1: y = 10; break;
case 2: y = 20;
}
/*--------------------------------------------------- 63 switch_default XFAIL */
switch (x) {
case 1: y = 10;
default: y = 99;
}
/*----------------------------------------------- 64 switch_fallthrough XFAIL */
switch (x) {
default: y = 0;
}
/*---------------------------------------------------- 65 switch_simple XFAIL */
switch (x) {
case 1: y = 10;
case 2: y = 20;
}
/*-------------------------------------------------------- 66 while_seq XFAIL */
while (a) { x = 1; }
while (b) { y = 2; }
/*----------------------------------------------------- 67 while_simple XFAIL */
while (x) { y = 1; }
