MALFORMED SUITE ARTIFACT (ceo s283h): fwctx_suite_malformed.{sno,ref} is an OLD-converter artifact with the blocks-before-one-liners swallowing defect (read_suite: "ref segment after seq 2 has 11 lines, too short to hold 62 trailing one-line entries") -- it has NEVER produced a SUITE_BOARD line and its -INCLUDE entries need the .inc files in this directory. Same class as the rtx11_dynvar/rtx_func_11 QA items; owned by corpus-crosscheck-probe-total-conversion QA. Moved out of the auto-discovered suite dir so the floor gate stops refusing on it.
