# beauty_suite `.ref` provenance — three drivers whose pins outrun the live SPITBOL oracle

**hq_P · 2026-08-29 · row `beauty-suite-ref-provenance` · ⛔ NOTHING IN THIS DIRECTORY WAS CHANGED. This file records
evidence for a ruling that is Lon's to make.**

## The situation

Three drivers **match their pinned `.ref` exactly** while the **live SPITBOL oracle refuses to run them at all**:

| driver | live `sbl -bf` says | site |
|---|---|---|
| `tree_driver` | `ERROR 067 -- array dimension is zero, negative or out of range` | `tree.sno:16` |
| `trace_driver` | `ERROR 243 -- function result in nreturn is not name` | line 59 |
| `ReadWrite_driver` | `ERROR 116 -- inappropriate file specification for input` | `ReadWrite.sno:9` |

All three reproduce on demand (re-verified 2026-08-29 against `/home/resources/x64/bin/sbl -bf`). In every case
**SCRIP runs FURTHER than the live oracle** and produces the pinned answer.

## 1. Provenance — established from git receipts, not inferred

`git log --follow` on each `.ref` terminates at the same commit:

```
04d21dc42  2026-03-29  M-G0-CORPUS-AUDIT: migrate SNOBOL4 smoke/feat/jvm_j3/beauty from one4all
d3d9079bd  2026-04-21  Rename beauty_*_driver.{sno,ref} and beauty_*_tracepoints.conf
4da0de78d  2026-05-20  rename: programs/snobol4/beauty/ → programs/snobol4/beauty_suite/
91e4465cc  2026-08-24  corpus: THE RE-GRID (Lon s272)
```

⭐ **Every commit after the first is a RENAME. All three files are BYTE-IDENTICAL to their 2026-03-29 import**
(verified by `git show 04d21dc42:<path> | cmp`).

⛔⭐ **CORRECTION (hq_P, same day, on Lon's question "which three sources?"): MY FIRST ANSWER TO "WHICH ENGINE" WAS
WRONG, AND THE RIGHT ANSWER WAS WRITTEN IN THE FILES ALL ALONG.** I said "none of ours — inherited wholesale,
predating the practice of pinning against an oracle here at all," and called this *stronger* than the s191
`prototype-array-dim` case. It is not stronger; **it is that case exactly.** Two of the three drivers name their
oracle in their own header:

```
*  Oracle: snobol4 -f -P256k -Idemo/inc driver.sno
```

`snobol4` is **CSNOBOL4**, not SPITBOL — SPITBOL is `sbl`, and `-f -P256k -I<dir>` is CSNOBOL4 flag style, not the
mandatory `-bf`. So these pins **were** produced by a named engine, and the name was sitting in a comment two lines
from the top. ⭐ **The git archaeology was correct but answered a narrower question than I asked of it:** `--follow`
established *when* the files arrived and that they are untouched since; it could not establish *which engine*, and I
filled that gap with an inference instead of reading the file. **The provenance was documentation, not history.**

⭐ **What survives the correction, and it is the load-bearing half:** the pins are graded against SPITBOL today while
having been generated by CSNOBOL4, and they are byte-identical to their 2026-03-29 import — so nothing has ever
re-derived them against the engine that now judges them.

**THE THREE SOURCES UNDER TEST**, each pulled in by its driver alongside a shared `global.sno`:

| driver | source | live-`sbl` refusal |
|---|---|---|
| `tree_driver.sno` | `tree.sno` | ERROR 067 at `tree.sno:16` |
| `trace_driver.sno` | `trace.sno` | ERROR 243 at line 59 |
| `ReadWrite_driver.sno` | `ReadWrite.sno` | ERROR 116 at `ReadWrite.sno:9` |

## 2. Adjudication — all three are (a) genuine SPITBOL restrictions, not (b) sbl-side limitations

Each code is in the SPITBOL v3.7 manual's own documented error table:

| code | manual line | text |
|---|---|---|
| 067 | 11380 | `ARRAY dimension is zero,negative or out of range` |
| 243 | 11572 | `Function result in NRETURN is not name` |
| 116 | 11434 | `Inappropriate file specification for INPUT` |

⛔ **None is an sbl implementation artifact.** The live oracle is enforcing documented SPITBOL semantics, and the
programs violate them. ⭐ **The consequence is the uncomfortable half: SCRIP is MORE PERMISSIVE than SPITBOL on these
three constructs, and the pinned `.ref` records that permissiveness as the expected answer.** The pin is not
"correct output from a different engine" — it is output SPITBOL says should never have been produced.

## 3. Why nothing was changed

- ⛔ **Re-pinning is a corpus-contract precedent, not a seat's call** — moving a pin *away* from an oracle is
  precisely the decision the row escalates. 17/17 M1 DOD currently depends on these pins.
- ⛔ **"Curing" SCRIP to match live sbl is equally out of scope** and would be a semantic narrowing, not a bug fix.
- ⭐ **The third option nobody has named** and which the evidence now makes available: mark the three
  **out-of-dialect** — the programs use constructs SPITBOL rejects, so they are not SNOBOL4-conformance witnesses at
  all, whatever they are useful for. That is a different disposition from re-pinning and does not touch a `.ref`.

**Asked of Lon, with this evidence attached, 2026-08-29. Disposition pending; until then these pins stand.**

## Related class

Same family as the stale `.xfail` markers (3 of 4 in `crosscheck/snocone` were false) and the self-pinned
`.expected` seat16 found on `write_canonical` — **a recorded expectation that outlived, or never had, the authority
it claims.** The shared cure is to re-derive against the oracle rather than inherit; the shared danger is that all
three read as green.
