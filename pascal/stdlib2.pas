program stdlib2(output);
var r: real;
begin
  r := sqrt(2.0);
  writeln(r:20);
  r := sin(1.0);
  writeln(r:20);
  r := cos(0.0);
  writeln(r:20);
  r := exp(1.0);
  writeln(r:20);
  r := ln(1.0);
  writeln(r:20);
  r := arctan(1.0);
  writeln(r:20)
end.
