// B10_num_le: <= succeeds when left <= right
a = 5;
b = 5;
if (a <= b) {
    OUTPUT = "le";
} else {
    OUTPUT = "not le";
}
a = 3;
if (a <= b) {
    OUTPUT = "le";
} else {
    OUTPUT = "not le";
}
a = 7;
if (a <= b) {
    OUTPUT = "le";
} else {
    OUTPUT = "not le";
}
