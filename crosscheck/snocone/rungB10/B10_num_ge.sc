// B10_num_ge: >= succeeds when left >= right
a = 5;
b = 5;
if (a >= b) {
    OUTPUT = "ge";
} else {
    OUTPUT = "not ge";
}
a = 8;
if (a >= b) {
    OUTPUT = "ge";
} else {
    OUTPUT = "not ge";
}
a = 2;
if (a >= b) {
    OUTPUT = "ge";
} else {
    OUTPUT = "not ge";
}
