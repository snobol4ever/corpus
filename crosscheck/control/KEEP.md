# KEEP — permanent standalone files, not suite-consolidated

- `expr_eval.sno`

Stdin-bearing (paired with `expr_eval.input`). The settled one-line/banner-
block suite format has no stdin-input concept, and hq_C ruled stdin-bearing
tests stay loose files **permanently** as their own scope-boundary category
(`corpus-suites-consolidation.task.md` LEDGER, seat07 2026-08-24: "input-
bearing tests stay loose files PERMANENTLY... `--skip`/`--skip-reason` is
APPROVED as the permanent handling"; reaffirmed seat08 2026-08-27,
"arith/control — CLOSED, permanent"). Not pending, not a gap — out of scope
by ruling.
