* arith_loop -- counts to 1000 by repeated increment and reports the total.
* bottleneck: counter increment; loop dispatch.
        total = 0
        step = 1
loop    total = total + 1
        step = LT(step, 1000) step + 1                  :S(loop)
        OUTPUT = 'arith_loop(1000) = ' total
END
