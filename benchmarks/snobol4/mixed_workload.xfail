XFAIL m4 ONLY -- PRE-EXISTING COMPILER SEGV, NOT A BENCHMARK DEFECT (minted 2026-08-19 s170).
  oracle sbl : check: 12100   PASS
  scrip m3   : check: 12100   PASS
  scrip m4   : SIGSEGV (rc=139, no output at all -- it dies before phase 1 prints)
NOT CAUSED BY THE HARNESS CONVERSION, measured both ways at pristine SCRIP b7e10d3c:
the PRE-conversion program (corpus a3604cc9, fixed-count legacy shape) SEGVs in m4 too, and
so does the same program with its loop count set to 0 -- ONE pass. Scale is not the trigger.
bench_sno_rtx.sh's s203 comment already recorded "mixed_workload segfaults on pristine main
rc=139"; nobody saw it on a board because an ungraded program is one nothing looks at.
ABLATED to three simultaneous ingredients -- remove ANY ONE and m4 is green (corpus/probe/mwseg/,
four witnesses, all four green in m3 and on the oracle):
  (1) a DEFINE'd function whose body CALLS ITSELF   (it is never even called at top level)
  (2) a pattern match with capture + destructive replacement
  (3) a TABLE store of the captured value
The one-ingredient pair mwseg_recfn_capture_table (SEGV) vs mwseg_flatfn_capture_table (green)
differs by the single self-call `F = N + F(N - 1)` versus `F = N + 1` -- that is the asm diff to
start from (ASM-DIFF-FIRST). Routed, not hunted: the BM-2 seat owns the corpus, not this class.
Record: FINDING-2026-08-19-s170-bm2-one-copy-and-two-m4-segv-classes-the-ungraded-were-hiding.md
