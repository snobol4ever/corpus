# DERIVED REF -- corpus/benchmarks/rebus/string_concat.reb
# Rebus has NO INDEPENDENT RIVAL IMPLEMENTATION, so this kernel's .ref cannot be cut from an oracle of
# its own language.  CEO-607 (2026-09-12, option (a)): a no-rival language's runtime ref is DERIVED and
# the derivation is RECORDED BESIDE IT, and the strongest derivation is a CROSS-LANGUAGE COMPUTATION of
# the same kernel under a real oracle -- not a hand sum.  Both witnesses are recorded below; the FIRST is
# the authority.  SCRIP's own output is a CHECK against this file, never the source of it.
#
# WITNESS 1 (authority) -- THE RIVAL PROGRAM AND ITS ORACLE'S OUTPUT
#   rival     SCRIP/scripts/fixtures/rebus_derivation/string_concat.sno   (the same kernel,
#             transliterated: 4000 single-character appends, then the SIZE of the result)
#   oracle    /home/resources/x64/bin/sbl -bf string_concat.sno < /dev/null   (SPITBOL, the SNOBOL4
#             correctness oracle -- resolved through lib_oracle_flags.sh, never a hand-built path)
#   output    4000
# WITNESS 2 (second witness) -- HAND ARITHMETIC
#   4000 iterations appending one character each to an initially empty string -> size 4000
#
# RE-PROVED ON EVERY RUN, NEVER TRUSTED AS A STATIC RECORD:
#   bash SCRIP/scripts/test_gate_rebus_derived_refs_match_their_rival_oracle.sh
