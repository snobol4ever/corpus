# DERIVED REF -- corpus/benchmarks/rebus/arith_loop.reb
# Rebus has NO INDEPENDENT RIVAL IMPLEMENTATION, so this kernel's .ref cannot be cut from an oracle of
# its own language.  CEO-607 (2026-09-12, option (a)): a no-rival language's runtime ref is DERIVED and
# the derivation is RECORDED BESIDE IT, and the strongest derivation is a CROSS-LANGUAGE COMPUTATION of
# the same kernel under a real oracle -- not a hand sum.  Both witnesses are recorded below; the FIRST is
# the authority.  SCRIP's own output is a CHECK against this file, never the source of it: a ref cut from
# the implementation it grades proves only that the implementation is deterministic.
#
# WITNESS 1 (authority) -- THE RIVAL PROGRAM AND ITS ORACLE'S OUTPUT
#   rival     SCRIP/scripts/fixtures/rebus_derivation/arith_loop.sno   (the same kernel, transliterated:
#             same accumulator, same bounds 1..300000, same single output)
#   oracle    /home/resources/x64/bin/sbl -bf arith_loop.sno < /dev/null   (SPITBOL, the SNOBOL4
#             correctness oracle -- resolved through lib_oracle_flags.sh, never a hand-built path)
#   output    45000150000
# WITNESS 2 (second witness) -- HAND ARITHMETIC
#   sum(1..300000) = 300000*300001/2 = 45000150000
#
# RE-PROVED ON EVERY RUN, NEVER TRUSTED AS A STATIC RECORD:
#   bash SCRIP/scripts/test_gate_rebus_derived_refs_match_their_rival_oracle.sh
# That gate re-runs the rival under the live oracle and diffs the result against arith_loop.ref, so this
# record cannot drift away from the thing it claims.  A static sidecar that nobody re-executes is a claim,
# not a measurement.
