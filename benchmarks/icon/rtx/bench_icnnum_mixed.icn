procedure main()
   local i, s, acc, t0;
   acc := 0;
   t0 := &time;
   every i := 1 to 6000000 do {
      s := string(i);
      acc +:= s + 1;
      };
   write("ms: ", &time - t0);
   write("acc: ", acc);
end
