# UNGRADED.tsv -- pat: shipped, GRADABLE, work owed before grading. THE PACKAGE LOCKDOWN.
# Columns: name<TAB>CLASS<TAB>what is owed (lib_inventory.sh's documented, gate-tested shape).
# Applies only to the ACCEPTANCE population (iso7185pat*); all 427 iso7185prt* rejection tests are graded.
# EMPTY ON PURPOSE, 2026-09-07 (cfo): the sole row, iso7185pat.pas TIMEOUT ("fpc -Miso does not finish
# within 60s; raise the bound or profile why"), was MEASURED, not carried over. The oracle does not finish
# at 300s either (ppcx64 at 100% CPU, no output). A prefix bisection stops at line 77, `label 0, 3, 9999,
# 0004;`, and a three-line program `program t(output); label 0; begin writeln(1) end.` reproduces it:
# fpc 3.2.2 -Miso (and -Mextendedpascal) loops forever on a label numbered 0, which ISO 7185 permits
# (labels 1, 4, 01, 0004, 9999 all compile or fail fast). That is the ORACLE's behaviour on a conforming
# program, so the row moved to UNGRADABLE.tsv as ORACLE_REFUSES with that reason; no bound cures it.
