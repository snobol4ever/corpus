# (all fsx_*.txt union fsx_c*) minus fsx_*.icn: every pattern names the fset.fixtures files only, so the listing never depends on
# what else ships in progs/ (hq_icon 2026-09-25)
fset	(fsx_*.txt++fsx_c*)--fsx_*.icn
