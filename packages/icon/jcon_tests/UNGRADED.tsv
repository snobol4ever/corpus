# UNGRADED.tsv -- jcon_tests: shipped, GRADABLE, ref not cut yet. This file must reach zero rows.
# THE PACKAGE LOCKDOWN (Lon 2026-09-06). Columns: path <TAB> CLASS <TAB> what is owed (lib_inventory.sh).
# EMPTY ON PURPOSE, 2026-09-07 (cfo): the sole row, link1.icn NEEDS_RUNNER_WIRING, is WIRED and GRADED. The
# runner now reads the modules a program `link`s from the program itself and builds it two-file in both modes,
# takes its argv from `link1.args` (a b c), and grades against `link1.ref`, cut from the shared icont/iconx
# oracle (/home/resources/icon-master) with that argv: `{{ a }} / {{ b }} / {{ c }}` on three lines, rc=0,
# byte-stable across two runs. Upstream ships no link1.std; `.ref` and `.args` are ours and say so in README.md.
#
# ⭐ FROM OUTSIDE_ARIZONA_BASELINE.tsv 2026-09-09 (hq_P, ceo CEO-470). TIMEOUT is WORK OWED, not a ruling:
# Arizona icont compiles both and it is OUR 30-second cap the run does not fit inside, so a bigger cap or a
# faster runtime brings them back. That is why they are here and not in UNGRADABLE.tsv -- the ceo's ruling
# is explicit that they are TIMEOUT-class and NOT oracle-refused.
# ⛔⭐ 2026-09-10 hq_P (ceo CEO-491, on Lon's order): lgint.icn and toby.icn LEFT this file for the GRADED
# population -- they are not work owed, they are reds with an owner (hq_I). Their vendored lgint.std/toby.std are
# JCON-provenance refs and say so in README.md § REF PROVENANCE. The TIMEOUT rows they leave behind were honest
# and still wrong in a way worth keeping: toby's oracle was never SLOW, it was WRONG (Arizona's `to` wraps past
# 9223372036854775807 and never stops), and a cap cannot tell a defect from a long run. Do not re-add them.
# ⛔⭐ 2026-09-10 hq_P (ceo CEO-487): geddump htprep prepro ARRIVED HERE FROM UNGRADABLE.tsv, where they sat as
# ORACLE_REFUSES.  The refusal was real and was OF OUR OWN FILE: each differs from its /home/resources/jcon-master
# upstream ONLY by semicolons our 826-file conversion appended, upstream compiles rc=0 under the same icont, and
# its output matches the vendored .std BYTE-FOR-BYTE -- so no .std re-cut is owed and the whole task is to
# re-vendor.  ⛔ A wrong UNGRADABLE hides work forever (lib_inventory.sh's tie-break); these three hid a one-sed
# repair behind a closed ruling because no row carried an UPSTREAM CONTROL.  hq_B holds the repair class.
geddump.icn	NEEDS_VENDORED_SOURCE	⛔ NOT AN ORACLE REFUSAL -- OUR VENDORING (corrected hq_P 2026-09-10 on ceo CEO-487, measured against /home/resources/jcon-master/test/geddump.icn). Our copy differs from upstream ONLY by the semicolons our 826-file conversion appended: ZERO residual lines after stripping a trailing `;`. Upstream compiles rc=0 under `icont -s` and, run against the shipped geddump.dat, its stdout MATCHES the vendored geddump.std BYTE-FOR-BYTE -- so ground truth exists, no .std re-cut is owed, and the only work is re-vendoring the semicolons out of the positions BOTH dialects reject. The row this replaces read ORACLE_REFUSES "a jcon-dialect source Arizona Icon does not accept" and was false because upstream was never asked.
htprep.icn	NEEDS_VENDORED_SOURCE	⛔ NOT AN ORACLE REFUSAL -- OUR VENDORING, same measurement as geddump (hq_P 2026-09-10): zero residual lines against /home/resources/jcon-master/test/htprep.icn after stripping a trailing `;`, upstream compiles rc=0 under `icont -s`, and its run against htprep.dat MATCHES the vendored htprep.std byte-for-byte. Our three refusals (Line 257 "missing then", 266 and 286 "missing right parenthesis") are semicolons the conversion put INSIDE an if condition and inside parenthesised continuations -- invalid in Arizona Icon AND in ours.
prepro.icn	NEEDS_VENDORED_SOURCE	⛔ OURS TOO, WHICH CORRECTS CEO-487's RULING THAT IT STAYS OUTSIDE (measured hq_P 2026-09-10). `$undef ghi` with ONE argument is ordinary Arizona Icon, not a jcon extension: upstream test/prepro.icn compiles rc=0 and runs clean under icont once its shipped prepro.dat companion sits beside it -- a bare staged copy fails "prepro.dat: cannot open", which is the STAGING and not the preprocessor, and that is the error CEO-487 read as a refusal. Our "$undef: too many arguments" IS the `;` the conversion appended to 17 directive lines in prepro.icn and 2 in prepro.dat. PROVEN BY REPAIR: strip only those 19 semicolons and icont compiles rc=0, runs rc=0, and the output matches the vendored prepro.std byte-for-byte.
