# UNGRADED.tsv -- csnobol4_suite: shipped, GRADABLE, ref not cut yet. THE PACKAGE LOCKDOWN (Lon
# 2026-09-06). Columns: path<TAB>CLASS<TAB>what is owed (lib_inventory.sh's own documented and
# gate-tested shape; CLASS added 2026-09-06 -- these two rows predate the shared instrument landing).
preload1.sno	WIRING_GAP	needs a per-test argv/-L mechanism in test_snobol4_csnobol4_suite.sh (same class as the existing argv_for() map used for genc). Measured: `csnobol4 -b -Laa/aa.sno preload1.sno` (exactly tests.in's own `reg -Laa/aa.sno preload1.sno`) -> stdout "aa\n", rc=0, deterministic (re-run twice, identical). Ref not yet cut pending that wiring — see FINDING-2026-09-06-seat03.
preload2.sno	WIRING_GAP	needs a per-test argv/-L mechanism (two -L flags). Measured: `csnobol4 -b -Laa/aa.sno -Lbb/bb.sno preload2.sno` (exactly tests.in's own `reg -Laa/aa.sno -Lbb/bb.sno preload2.sno`) -> stdout "aa\nbb\n", rc=0, deterministic. Ref not yet cut pending that wiring — see FINDING-2026-09-06-seat03.
