# UNGRADED.tsv -- csnobol4_suite: shipped, GRADABLE, ref not cut yet. THE PACKAGE LOCKDOWN (Lon
# 2026-09-06). Columns: path<TAB>CLASS<TAB>what is owed (lib_inventory.sh's own documented and
# gate-tested shape).
# EMPTY ON PURPOSE, 2026-09-06 (hq_S): preload1.sno and preload2.sno were this bucket's only two rows.
# Their pickup was TWO things and the sidecar named only one -- scrip had no -L flag at all (the argument
# fell through to the filename path), so there was no preload for a runner map to pass. Both landed
# together: -L in the driver, preload_for() in the runner, and their refs cut from csnobol4's own run of
# upstream tests.in's registration lines. They are GRADED now, and they are RED -- lowercase `output` in
# aa/aa.sno relies on csnobol4's default identifier folding, which SCRIP does not do (it is
# case-sensitive by SPITBOL semantics). That is a real, named divergence in the graded population
# instead of two rows hidden in an ungraded bucket, which is the whole point of the lockdown.
