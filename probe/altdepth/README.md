# probe/altdepth — the NON-LEAF ALT-carrier depth class (minted s127)

The s127 rung (SCRIP `88a50450`) admits the ALT-carrier resume ONLY for blobs whose single
unsealed choice node has ALL-LEAF siblings (the depth-exact class). These witnesses pin what
remains REFUSED — the class beauty's `Command` lives in — measured at mint time:

| witness | oracle | default (ALT_CARRIER on) | SCRIP_ALT_CARRIER=0 |
|---|---|---|---|
| alt_captarm_defer_red | `match ab` | **rc=139 rip-0 bomb** | `nomatch` (silent wrong) |
| alt_twochoice_defer_red | `nomatch` | **rc=139 rip-0 bomb** | `nomatch` (ORACLE-EQUAL) |

⛔ **alt_twochoice_defer_red is the sharpened hazard**: legacy was CORRECT here and the s127
default bombs — the admitted af edge cascades `jne -> jmp [rsp]` into a refused blob whose
β dead-ends at ω without consuming its retained two-record interior; the second lap jumps
through an arm record's cursor dword. Not present anywhere in the 318 crosscheck corpus
(measured s127, zero movers) and beauty is crash-free both arms — but the boundary is now a
witness, not an argument.

Both cure under the named next rung: rbp-relative choice records in the blob activation frame
(+ zd cross-blob pricing taught blob_frame_bytes FIRST — see the ⛔ conviction at
emit.cpp blob_frame_bytes for the measured order-of-operations proof).
