*  RED, CHECKED IN RED (law 0d).  ***THE RESIDUAL AFTER s192 FENCE-RTAIL, AND THE MINIMAL SPELLING OF WHAT
*  STILL FAILS crosscheck 145.***  The carrier IS published here -- SCRIP_RESUME_WHY=1 reads body_root_op=57
*  (ARBNO) tier=1 f1r=0, identical to the GREEN rty_fence_capdefer_right_ctl beside it -- so this is NOT the
*  carrier-selection class s192 cured.  The two differ by ONE token: the capture on element 0.  Both modes
*  agree ('fail'), so it is a plain wrong answer, not a divergence.  NEXT ROW OWNS THIS: what the blob's
*  beta does AFTER it lands a correctly published ARBNO beta.
        num   = SPAN('0123456789')
        expr  = LEN(1) ARBNO(FENCE('+') num . LAST)
        '1+2+3' POS(0) expr RPOS(0)                     :S(Y)F(N)
Y OUTPUT = 'match'                                      :(END)
N OUTPUT = 'fail'
END
