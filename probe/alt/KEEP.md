# KEEP.md — probe/alt

Per `probe-consolidate-m1-and-small` (parent: `corpus-suites-consolidation`).

`alt_defer_recur_af.sno` is **89 source lines** — over Lon's SIZE RULING ceiling
(`corpus-suites-consolidation.task.md` LEDGER, ceo 2026-08-27: "≤40 source lines → suite
entry ... >40 lines → converter's judgment, standalone-keep allowed ... recorded in the
family's KEEP.md with its line count"). Left as a standalone file, unconverted, unmoved.

| file | lines | reason |
|---|---|---|
| alt_defer_recur_af.sno | 89 | exceeds the 40-line suite-entry ceiling; converter's judgment to keep standalone rather than force into a banner block |

## Machine-readable exception list (DONE-WHEN mirror)

⛔ Mirrors `probe/conformance/KEEP.md`'s fix (SCRIP `f3dc4672a`) -- this row's DONE-WHEN
greps this section, not the prose above, for `- <path>` lines. Add a line here in the SAME
edit that adds a file to this KEEP.md; remove one here in the SAME edit that converts a file
out of it.

- probe/alt/alt_defer_recur_af.sno
