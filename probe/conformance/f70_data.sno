        DATA('COMPLEX(REAL,IMAG)')
        X = COMPLEX(3.2, 2.0)
        OUTPUT = REAL(X)
        OUTPUT = IMAG(X)
        REAL(X) = 1.0
        IMAG(X) = IMAG(X) / 2
        OUTPUT = REAL(X)
        OUTPUT = IMAG(X)
        OUTPUT = DATATYPE(X)
END
