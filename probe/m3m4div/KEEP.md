# KEEP.md — probe/m3m4div

Per `probe-consolidate-m1-and-small` (parent: `corpus-suites-consolidation`).

The only file in this directory, `alpha_scan_pollution.sno`, is **375 lines** -- far over
Lon's 40-line suite-entry ceiling. Nothing converted; nothing to git rm. Left exactly as-is.

| file | lines | reason |
|---|---|---|
| alpha_scan_pollution.sno | 375 | exceeds the 40-line suite-entry ceiling by nearly 10x -- clearly a standalone witness, not suite material |

## Machine-readable exception list (DONE-WHEN mirror)

⛔ Mirrors `probe/conformance/KEEP.md`'s fix (SCRIP `f3dc4672a`) -- this row's DONE-WHEN
greps this section, not the prose above, for `- <path>` lines. Add a line here in the SAME
edit that adds a file to this KEEP.md; remove one here in the SAME edit that converts a file
out of it.

- probe/m3m4div/alpha_scan_pollution.sno
