program stdlib2(output);
var r : real;
begin
  r := sin(1.0);
  writeln(r);
  r := cos(0.0);
  writeln(r);
  r := sqrt(2.0);
  writeln(r);
  r := arctan(1.0);
  writeln(r);
  r := exp(1.0);
  writeln(r);
  r := ln(r);
  writeln(r)
end.
